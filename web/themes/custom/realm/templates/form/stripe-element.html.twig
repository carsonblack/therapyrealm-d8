<div id="{{ id }}-card-element" class="{{ classes }}">
    {# a Stripe Element will be inserted here. #}
</div>

{#  Used to display form errors #}
<div id="{{ id }}-card-errors" class="sripe-card-errors u-label u-label--lg u-label-danger g-mb-20" role="alert"></div>

{#  Render the element itself #}
{{ children }}
